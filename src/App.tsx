import { BrowserRouter } from "react-router-dom";
import RoutesRoot from "routes/RoutesRoot";

function App() {
	return (
		<BrowserRouter>
			<RoutesRoot />
		</BrowserRouter>
	);
}

export default App;
